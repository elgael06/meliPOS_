//librerias
import React from 'react';
import { connect } from 'react-redux';

const Monitors =()=>{

    return (<div>
        <label>Este es el Monitors...</label>
    </div>);
}

const mapStateProps=()=>({

});

const mapDispatchToProps = dispatch =>({

});

export default connect(mapStateProps,mapDispatchToProps)(Monitors);
