//librerias
import React from 'react';
import { connect } from 'react-redux';

const Sales =()=>{

    return (<div>
        <label>Este es el Sales...</label>
    </div>);
}

const mapStateProps=()=>({

});

const mapDispatchToProps = dispatch =>({

});

export default connect(mapStateProps,mapDispatchToProps)(Sales);
